import { Appointments } from './app/interfaces';

export const CalenderData: any = [
  {
    year: 2019,
    months: [
      {
        name: 'January',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'February',
        days: [...Array(28).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'March',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'April',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'May',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'June',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'July',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'August',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'September',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'October',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'November',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'December',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },
    ],
  },

  {
    year: 2020,
    months: [
      {
        name: 'January',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'February',
        days: [...Array(28).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'March',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'April',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'May',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'June',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'July',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'August',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'September',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'October',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'November',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'December',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },
    ],
  },

  {
    year: 2021,
    months: [
      {
        name: 'January',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'February',
        days: [...Array(28).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'March',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'April',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'May',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'June',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'July',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'August',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'September',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'October',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'November',
        days: [...Array(30).keys()].map((x) => {
          return { days: x };
        }),
      },

      {
        name: 'December',
        days: [...Array(31).keys()].map((x) => {
          return { days: x };
        }),
      },
    ],
  },
];

export const years: number[] = [2021, 2022, 2023];
export const months: any = [
  {
    name: 'January',
    days: [...Array(31).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'February',
    days: [...Array(28).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'March',
    days: [...Array(31).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'April',
    days: [...Array(30).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'May',
    days: [...Array(31).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'June',
    days: [...Array(30).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'July',
    days: [...Array(31).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'August',
    days: [...Array(31).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'September',
    days: [...Array(30).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'October',
    days: [...Array(31).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'November',
    days: [...Array(30).keys()].map((x) => {
      return { days: x };
    }),
  },

  {
    name: 'December',
    days: [...Array(31).keys()].map((x) => {
      return { days: x };
    }),
  },
];

export const monthsSerial: any = {
  January: '01',
  February: '02',
  March: '03',
  April: '04',
  May: '05',
  June: '06',
  July: '07',
  August: '08',
  September: '09',
  October: '10',
  November: '11',
  December: '12',
};

export const allMonths = [
  'January',
  'February',
  'March',
  'April',
  'May',
  'June',
  'July',
  'August',
  'September',
  'October',
  'November',
  'December',
];

export const getCurrentMonth = () => {
  const serial = [
    'January',
    'February',
    'March',
    'April',
    'May',
    'June',
    'July',
    'August',
    'September',
    'October',
    'November',
    'December',
  ];
  const currentMonthNo: number = new Date().getMonth();

  return serial[currentMonthNo];
};

const allData = [
  {
    age: '20',
    date: '2022-08-01',
    gender: 'male',
    name: 'demo',
    time: '18:40',
  },
  {
    age: '20',
    date: '2022-09-01',
    gender: 'male',
    name: 'demo2',
    time: '16:40',
  },
  {
    age: '20',
    date: '2022-10-01',
    gender: 'male',
    name: 'demo2',
    time: '16:40',
  },
];
export const getData = () => {
  return allData;
};

const formatDate = (date: any) => {
  var d = new Date(date),
    month = '' + (d.getMonth() + 1),
    day = '' + d.getDate(),
    year = d.getFullYear();

  if (month.length < 2) month = '0' + month;
  if (day.length < 2) day = '0' + day;

  return [year, month, day].join('-');
};

export const setData = async (data: Appointments) => {
  const preparedData = {
    ...data,
    date: formatDate(data.date),
  };

  allData.push(preparedData);
  return allData;
};
